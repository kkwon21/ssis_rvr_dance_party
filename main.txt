async function startProgram() {
	// This is our sample code for making the RVR drive in a square.

	await roll(0,60,1.9) //Drive for two seconds at a speed of 50 at a heading of 0 degrees.
	await stopRoll()
	await roll(90,60,2.6) //Continue for each corner of the square
	await stopRoll()
	await roll(180,60,2.6)
	await stopRoll()
	await roll(270,60,2.5)
	await stopRoll()
	
	await roll(0,1,0.5) //This realigns the robot at the end of the movement.
	
}

//This is a good start. Pull this modification of your code into your project.
